# Listen on localhost only (dev)
listener 1883 127.0.0.1

# Dev-only: allow local anonymous clients
allow_anonymous true

# Keep messages across restarts (optional)
persistence true
persistence_location C:\Users\andre\mosq_data\